			//echo "<pre>";
			//ordenancia
			
/*			$cleanedArray = $util->orderMultiDimensionalArray ($cleanedArray, "responsable");
*/		
/*			foreach($cleanedArray as $key => $value)
			{
				if($value["tipoPersonal"] == "Contador")
				{
					$sortedArray["Contador"][] = $value;
				}

				if($value["tipoPersonal"] == "Auxiliar")
				{
					$sortedArray["Auxiliar"][] = $value;
				}

				if($value["tipoPersonal"] == "Supervisor")
				{
					$sortedArray["Supervisor"][] = $value;
				}

				if($value["tipoPersonal"] == "Gerente")
				{
					$sortedArray["Gerente"][] = $value;
				}

				if($value["tipoPersonal"] == "Socio")
				{
					$sortedArray["Socio"][] = $value;
				}

				if($value["tipoPersonal"] == "Recepcion")
				{
					$sortedArray["Recepcion"][] = $value;
				}

				if($value["tipoPersonal"] == "Asistente")
				{
					$sortedArray["Asistente"][] = $value;
				}

				if($value["tipoPersonal"] == "Nomina")
				{
					$sortedArray["Nomina"][] = $value;
				}
				
				
			}
			
			if(!is_array($sortedArray["Socio"])) {
				$sortedArray["Socio"] = array();
			}

			if(!is_array($sortedArray["Gerente"])) {
				$sortedArray["Gerente"] = array();
			}

			if(!is_array($sortedArray["Supervisor"])) {
				$sortedArray["Supervisor"] = array();
			}

			if(!is_array($sortedArray["Contador"])) {
				$sortedArray["Contador"] = array();
			}

			if(!is_array($sortedArray["Auxiliar"])) {
				$sortedArray["Auxiliar"] = array();
			}

			if(!is_array($sortedArray["Nomina"])) {
				$sortedArray["Nomina"] = array();
			}

			if(!is_array($sortedArray["Asistente"])) {
				$sortedArray["Asistente"] = array();
			}

			if(!is_array($sortedArray["Recepcion"])) {
				$sortedArray["Recepcion"] = array();
			}

			
			$cleanedArray = array_merge(
				$sortedArray["Socio"], 
				$sortedArray["Gerente"], 
				$sortedArray["Supervisor"], 
				$sortedArray["Contador"], 
				$sortedArray["Auxiliar"], 
				$sortedArray["Nomina"], 
				$sortedArray["Asistente"], 
				$sortedArray["Recepcion"] 
			);*/
//exit;			
		//	echo "<pre>";